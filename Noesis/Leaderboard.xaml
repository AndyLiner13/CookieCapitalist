<!-- 
  Leaderboard - Stats and leaderboard page (Screen Overlay)
  Display Mode: Screen Overlay, Render Order: 5
  Input Mode: Interactive, nonblocking
  Visibility controlled by TypeScript based on current page
-->

<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DataContext="Leaderboard_DataSet"
  d:DesignWidth="393"
  d:DesignHeight="852">

<Page.Resources>
  <ResourceDictionary>
    <BooleanToVisibilityConverter x:Key="BoolToVis"/>
  </ResourceDictionary>
</Page.Resources>

<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
      Visibility="{Binding isVisible, Converter={StaticResource BoolToVis}}">
  
  <Grid.RowDefinitions>
    <RowDefinition Height="143"/>  <!-- Header spacer -->
    <RowDefinition Height="*"/>     <!-- Content area -->
    <RowDefinition Height="124"/>   <!-- Footer spacer -->
  </Grid.RowDefinitions>
  
  <!-- Leaderboard Content Area -->
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Padding="8,8,8,8">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
      
      <!-- Leaderboard Entries using ItemsControl -->
      <ItemsControl ItemsSource="{Binding leaderboard}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Width="377" Height="80" CornerRadius="8" Background="#252525" Padding="8" Margin="0,0,0,8">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="171"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                  <Image Source="{Binding avatar}" Width="64" Height="64" Stretch="UniformToFill"/>
                  <TextBlock Text="{Binding username}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" Margin="26,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="{Binding score}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,8,0"/>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      
    </StackPanel>
  </ScrollViewer>

</Grid>
</Page>
