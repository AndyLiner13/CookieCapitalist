<!-- 
  Shop - Upgrade shop page (Screen Overlay)
  Display Mode: Screen Overlay, Render Order: 5
  Input Mode: Interactive, nonblocking
  Visibility controlled by TypeScript based on current page
-->

<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DataContext="Shop_DataSet"
  d:DesignWidth="393"
  d:DesignHeight="852">

<Page.Resources>
  <ResourceDictionary>
    <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    
    <!-- Invisible button style -->
    <Style x:Key="InvisibleButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="BorderBrush" Value="Transparent"/>
      <Setter Property="Padding" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="Transparent">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </ResourceDictionary>
</Page.Resources>

<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
      Visibility="{Binding isVisible, Converter={StaticResource BoolToVis}}">
  
  <Grid.RowDefinitions>
    <RowDefinition Height="143"/>  <!-- Header spacer -->
    <RowDefinition Height="*"/>     <!-- Content area -->
    <RowDefinition Height="124"/>   <!-- Footer spacer -->
  </Grid.RowDefinitions>
  
  <!-- Shop Content Area -->
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" Padding="0,8,0,8">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,0,16" ClipToBounds="False">
      
      <!-- Upgrade Card 1: Clicker -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/ClickerImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding clicker.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding clicker.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding clicker.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding clicker.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding clicker.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 2: Grandma -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/GrandmaImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding grandma.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding grandma.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding grandma.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding grandma.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding grandma.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 3: Farm -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/FarmImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding farm.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding farm.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding farm.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding farm.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding farm.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 4: Factory -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/FactoryImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding factory.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding factory.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding factory.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding factory.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding factory.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 5: Lab -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/LabImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding lab.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding lab.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding lab.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding lab.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding lab.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 6: Fab -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/FabImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding fab.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding fab.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding fab.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding fab.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding fab.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
      <!-- Upgrade Card 7: Planet -->
      <Border Width="371" Height="119" CornerRadius="8" Background="#a6000000" Padding="16" Margin="0,0,0,8">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Image Source="Images/PlanetImage.png" Width="87" Height="87" Stretch="UniformToFill"/>
          <StackPanel Orientation="Vertical" Width="240" Margin="12,0,0,0">
            <Border Width="240" Height="30" CornerRadius="5" Background="#515151" HorizontalAlignment="Stretch" ClipToBounds="True">
              <TextBlock Text="{Binding planet.name}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </Border>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
              <Button Width="160" Height="47" Style="{StaticResource InvisibleButtonStyle}" Command="{Binding planet.buyCommand}" RenderTransformOrigin="0.5,0.5">
                <Border Width="160" Height="47" CornerRadius="5">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Background" Value="#fa9750"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding planet.canAfford}" Value="False">
                          <Setter Property="Background" Value="#515151"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding planet.price}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
              </Button>
              <Border Width="75" Height="39" CornerRadius="5" Background="#515151" Margin="5,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding planet.owned}" FontFamily="Fonts/#Anton-Regular" FontSize="20" Foreground="#ffffff" HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </Border>
      
    </StackPanel>
  </ScrollViewer>

</Grid>
</Page>
