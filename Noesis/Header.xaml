<!-- 
  Header - Cookie count and CPS display (Screen Overlay)
  Display Mode: Screen Overlay, Render Order: 10 (top layer)
  Input Mode: No Interaction (display only)
  Shows cookie count and cookies per second at top of screen
-->

<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DataContext="Header_DataSet"
  d:DesignWidth="393"
  d:DesignHeight="852">

<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
  
  <!-- Header bar at top -->
  <Border HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="#80000000" Padding="16,58,16,8" Height="143">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
      <TextBlock Text="{Binding cookieCount}" FontFamily="Fonts/#Anton-Regular" FontSize="36" Foreground="#ffffff" TextAlignment="Center"/>
      <TextBlock Text="{Binding cookiesPerSecond}" FontFamily="Fonts/#Anton-Regular" FontSize="15" Foreground="#ffffff" TextAlignment="Center"/>
    </StackPanel>
  </Border>

</Grid>
</Page>
